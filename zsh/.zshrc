export EDITOR=nvim
alias vim="nvim" # No going back
alias cat="bat" # Pretty cat with syntax highlighting
alias ll="ls -la"

# Reload shell quickly
alias reload="source ~/.zshrc"

# Search shell history
bindkey '^r' history-incremental-search-backward
